## Contexte

Le serveur nanos world démarre et les packages suivants chargent correctement :

- `gr-core`
- `gr-database`
- `gr-characters`

Actuellement, `gr-database` affiche encore :

```txt
has_password=false
auto_connect=false